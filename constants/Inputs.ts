import { StyleSheet } from 'react-native';

export const INPUTS_STYLES = StyleSheet.create({
    input: {
        width: 300,
        height: 50,
        borderColor: '#808080',
        borderWidth: 1,
        borderRadius: 10,
        paddingHorizontal: 15,
        marginVertical: 10,
        fontSize: 16,
        backgroundColor: '#fff',
      },
});
